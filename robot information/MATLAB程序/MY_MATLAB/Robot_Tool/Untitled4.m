% 假设有一组三维向量存储在矩阵V中（每一列代表一个向量）
V = [1, 3, 2; 2, 2, 1; 0, 1, 3];

% 创建一个新图
figure;

% 使用quiver3函数绘制一组向量
quiver3([0;0;0], [0;0;0], [0;0;0], V(:,1), V(:,2), V(:,3),0);

% 添加标签和标题
xlabel('X轴');
ylabel('Y轴');
zlabel('Z轴');
title('绘制一组向量示例');

% 设置坐标轴相等

